# mcp-puppeteer-browser
Fork of official Reference server but in a single repo for easier installation
install
```
npm install
```

build
```
npm run build
```

config
```
{
  "mcpServers": {
    "mcp-puppeteer-browser": {
      "command": "node",
      "args": ["dist/index.js"]
    }
  }
}
```
